nav(class="navbar navbar-expand-lg bg-body-tertiary" style={padding:'0px'})
  div(class="container-fluid", style={background: '#FDAC60'})
    a(class="navbar-brand" href="/")
      img(src="/logos/Logo1.png" alt="Logo" width="50vw" height="40vh" class="d-inline-block align-text-top")
    div(class="collapse navbar-collapse" id="navbarNavDropdown")
      ul(class="navbar-nav")
        li(class="nav-item")
          a(class="nav-link" class=(selected_nav_link == "inicio") ? "active" : "" aria-current="page" href="/") Inicio
        li(class="nav-item dropdown")
          a(class="nav-link dropdown-toggle" class=(selected_nav_link == "mi-cuenta") ? "active" : "" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false") Mi cuenta
          ul(class="dropdown-menu")
            if !sesion.nombre
              li
                a(class="dropdown-item" href="/sesion/iniciar") Iniciar sesi&oacute;n
            else
              if sesion.nivel == 1
                li 
                  a(class="dropdown-item" href="/usuario/misMascotas") Mis mascotas
              li
                a(class="dropdown-item" href="/sesion/cerrar") Cerrar sesi&oacute;n
        if sesion.nivel == 2
          li(class="nav-item")
            a(class="nav-link" class=(selected_nav_link == "clientes") ? "active" : "" href="/clientes") Clientes
          li(class="nav-item")
            a(class="nav-link" class=(selected_nav_link == "perros") ? "active" : "" href="/perros") Perros
        li(class="nav-item dropdown")
          a(class="nav-link dropdown-toggle" class=(selected_nav_link == "publicaciones") ? "active" : "" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false") Publicaciones
          ul(class="dropdown-menu")
            li
              a(class="dropdown-item" href="/servicios") Servicios
            li
              a(class="dropdown-item" href="/adopciones") Adopciones
            if sesion.nivel > 0
              li 
                a(class="dropdown-item" href="/donaciones") Donaciones
        if sesion.nivel == 1 || sesion.nivel == 2
          li(class="nav-item dropdown")
            a(class="nav-link dropdown-toggle" class=(selected_nav_link == "turnos") ? "active" : "" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false") Turnos
            ul(class="dropdown-menu")
              li
                a(class="dropdown-item" href="#") Solicitudes
              if sesion.nivel == 1
                li
                  a(class="dropdown-item" href="/turno/solicitar") Solicitar turno
              li
                a(class="dropdown-item" href="#") Próximos
    if sesion.nombre
      = sesion.nombre+" "+sesion.apellido
    else
      .
        An&oacute;nimo
unless selected_nav_link == "inicio"
  br
  br